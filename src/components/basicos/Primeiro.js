export default function Primeiro() {
    const msg = 'Seja bem vindo'
    return (
        <div>
            <h2>Primeiro Componente!</h2>
            <p>{ msg }</p>
        </div>
    )
}

/**
 * componentes- criar com a primeira letra maiuscula

indentar na linha seguinto só com parentes 
return (
)
 */